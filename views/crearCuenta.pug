doctype html
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario 
            h1 Create UpTask Account

            if locals.mensajes 
              .alertas 
                each categoria in Object.keys(locals.mensajes)
                  each error in mensajes[categoria] 
                    .alerta(class=`${categoria}`)
                      p= error
            //- if errores 
            //-     each error in errores 
            //-         p.error.alerta= error.message

            form#fomulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo  
                    label(for="email")  E-mail:
                    input(type="email" name="email" placeholder="Your Email" value= email required)
                .campo  
                    label(for="password")  Password:
                    input(type="password" name="password" placeholder="Your Password" required)
                .campo.enviar
                    input.boton(type="submit" value="Sign Up")
                .campo
                    a(href="/iniciar-sesion") Go back to Login
        
        include partials/scripts